---
name: test-coverage-review
description: 根据代码 diff、覆盖率配置和风险路径识别未覆盖分支、异常路径与缺失测试。用于代码审批以及覆盖率门禁变更。
---

# 测试覆盖审查

关注变更风险与测试证据的对应关系，不把单一覆盖率百分比当成质量结论。没有真实覆盖率报告时，明确标注为静态推断。

## 审查流程

1. 从 diff 提取新增/修改的函数、分支、状态转换、异常处理、权限判断和外部调用。
2. 搜索对应测试，建立“变更路径 → 用例/断言 → 执行证据”映射；没有映射的路径列为缺口。
3. 优先检查错误、超时、空值、边界、并发、重试、回滚和权限失败路径，而不是只看 happy path。
4. 检查覆盖率工具和阈值是否覆盖正确的源码范围，排除项是否有理由且没有排除业务代码。
5. 检查变更覆盖门禁是否只要求行覆盖，是否遗漏分支、函数、条件或关键模块门禁。
6. 区分未测试代码、不可达代码和生成/供应商代码，不能把三者混为一谈。

## 反模式

- 只提高阈值或补一条浅断言来掩盖缺口。
- 大面积增加 `coverage ignore`、`skip`、`only` 或空测试。
- 只验证返回值，不验证副作用、状态和错误契约。
- 测试存在但没有被 CI 收集或报告。

## 输出

输出缺口清单、风险等级、受影响路径、现有证据和建议用例。建议应能直接转成回归测试，不要求审批阶段自动生成或运行测试。

## 覆盖模型

为每个受影响符号记录：变更文件/行号、函数或状态、正常/边界/异常/权限/并发路径、测试文件与断言、运行命令/提交/退出码，以及“已证明覆盖、静态推断、不可达、生成代码或证据不足”。存在测试文件不等于覆盖，必须确认断言能区分典型错误实现。

## 变更类型到测试要求

- 新分支：检查每条分支和边界，尤其默认与异常回退。
- 状态机：覆盖合法/非法转换、重复事件、并发事件和恢复。
- 外部调用：覆盖超时、空响应、部分失败、重试耗尽和清理。
- 权限/租户：覆盖允许、拒绝、跨资源和缺少上下文身份。
- schema/DTO：覆盖缺失、类型改变、未知字段、版本和错误格式。

读取覆盖率配置、source map、排除项和变更覆盖分析细节时加载 `references/source-1.md`；需要具体框架配置时再按章节加载 `references/source-2.md`。
